# SAFE TESTING CONFIGURATION
# Uses non-conflicting ports to avoid disrupting production services

# Database Configuration
POSTGRES_DB=v2test_db
POSTGRES_USER=v2test_user
POSTGRES_PASSWORD=v2test_password_change_me
POSTGRES_PORT=5433  # Different from default 5432

# Redis Configuration  
REDIS_PORT=6380  # Different from default 6379

# Adminer
ADMINER_PORT=8081  # Different from default 8080

# Application Ports (when ready)
BACKEND_PORT=4000  # Different from 3000
FRONTEND_PORT=4001  # Different from 3001

# Test Environment
NODE_ENV=test