version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: dev-lowebi-postgres
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-lowebi_db}
      POSTGRES_USER: ${POSTGRES_USER:-lowebi_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lowebi_pass_2024}
    ports:
      - "7613:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - dev-lowebi-network
    restart: unless-stopped

  # Redis for Queue Management
  redis:
    image: redis:7-alpine
    container_name: dev-lowebi-redis
    ports:
      - "7614:6379"
    volumes:
      - redis_data:/data
    networks:
      - dev-lowebi-network
    restart: unless-stopped

  # Adminer for Database Management (Development)
  adminer:
    image: adminer:latest
    container_name: dev-lowebi-adminer
    ports:
      - "7615:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    networks:
      - dev-lowebi-network
    restart: unless-stopped

  # NestJS Backend (Development)
  backend:
    build:
      context: ../../apps/backend
      dockerfile: Dockerfile.dev
    container_name: dev-lowebi-backend
    environment:
      NODE_ENV: development
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: lowebi_user
      DATABASE_PASSWORD: lowebi_pass_2024
      DATABASE_NAME: lowebi_db
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: your-super-secret-jwt-key-change-in-production
      PORT: 3000
    ports:
      - "7610:3000"
    volumes:
      - ../backend:/app
      - /app/node_modules
    depends_on:
      - postgres
      - redis
    networks:
      - dev-lowebi-network
    restart: unless-stopped
    command: npm run start:dev

  # Next.js Frontend (Development)
  frontend:
    build:
      context: ../../apps/frontend
      dockerfile: Dockerfile.dev
    container_name: dev-lowebi-frontend
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:3000
    ports:
      - "7611:3000"
    volumes:
      - ../frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    networks:
      - dev-lowebi-network
    restart: unless-stopped
    command: npm run dev

networks:
  dev-lowebi-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data: