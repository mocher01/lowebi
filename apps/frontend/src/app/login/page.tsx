import CustomerLoginForm from '@/components/auth/customer-login-form';

export default function LoginPage() {
  return <CustomerLoginForm />;
}

export const metadata = {
  title: 'Sign In - LOGEN',
  description: 'Sign in to your LOGEN account',
};